# Independent moving-phase audit of the fixed admissible witness

2026-09-06, post-screen. The trial10 failed-state K4 LP witness satisfies
sampled edge/production constraints on its16/96 unsplit-angular mesh and
independent fixed rays. Rates vary about0.00718 across the two LP grids;
therefore do NOT turn it into a smooth downstream closure. Before any such
proposal, test the original weak/global equations with independent phase
angular splitting at the SAME stored rates and flux amplitudes.

Select the immutable fine witness at trial10's reproduced rejected8-step
endpoint. No re-optimization or refitting. Construct fully phase-split
radial4/angular8 and8/16 operators, including scalar mixing angular events.
Use their first5+2m rows: all original global and scalar weak equations with
solenoidal flux contributions. The remaining hard boundary rows belong to
the rejected hard-moment model and are NOT equations for this witness.
Its original hard-moment solution is not adopted or used for rates.

Compare the fixed candidate's normalized residual in every retained row
with1e-8 and matrix/right refinement with1e-5. Independently calculate its
full-vector production, actual mass/heat boundary balances and3 pointwise
edge residuals with the fixed candidate, not the hard-moment solution.
Require the original signs,5% pointwise and curvature gates. Scalar
diffusivity/rates/flux amplitudes remain unchanged.

Additionally compare actual advective5+2m moment central differences with
their fixed-field Jacobian times the candidate state rates. Use a two-sided
distance chosen so every encoded rate displacement is<=1e-6 and every
sampled shape change is safely within the remaining0.1 logshape margin;
then halve it. Require FD/Jacobian and step-refinement errors<=1e-5,
normalized by max(abs(analytic derivative),1). Do not use a boundary
refit/reprojection during these verification-only field perturbations.

Any numerical/physical failure stays a failure of this frozen witness.
Passing is not a turbulence closure, downstream or observed validation.
The independent meshes can be costly; keep progress snapshots and hashes,
refuse overwrite and preserve all prior failed prototypes.
