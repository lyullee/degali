# 실제 좌우 온도 비대칭과 관측 풍향 — 2026-09-06 13:40 KST

## 완료 결과

전체 비-slow 회귀는 13:11:05에 종료했다. 246 Python 파일의 고정 스냅샷에
817 PASS / 128 SKIP / 58 deselected, 실패 0이며 실행 중 변경이 없었다.
기록은 `reference/preslhy/stage2_regression_2026-09-06_1300/complete.json`.
이는 측정된 코드 커버리지나 Linux/slow 오라클 완료가 아니다. 이후 좌우 감사
8검사와 주변공기 벡터 연산자 8검사는 별도 통과했으며 833 전체회귀로 합산하지 않는다.

같은 x/z/표본시각을 가진 원래 좌우 4쌍(41센서 중 8개)을 모두 비교했다.
시험10/23의 x=4m에서 왼쪽−오른쪽 온도차는 평균 44.52982/25.51329K이며,
두 시험 모두 같은 시각 표본의 100%에서 오른쪽이 더 차가웠다. 현재 평면
모델은 좌우에 같은 값을 준다. 어느 공통 예측값에도 성립하는 삼각부등식으로
이 4쌍만의 전체41 MAE 하한은 minimum 3.57637K, mean 1.89289K이다.
측정·비정상·센서 불확실성을 포함한 관측 비교 하한이지 순수 물리오차 하한은 아니다.
쌍의 중간값을 모델 목표로 선택하거나 개별 최저값을 동시 관측처럼 다루지 않았다.

## 원문과 원시 바람

PRESLHY D3.6 V1.20 PDF p31–33, p44, p48 및 원본 XLSX를 확인했다.
분사 방위는 북쪽 기준 약75도이며 기록 풍향은 wind-FROM이다. 오른쪽 축
방위는165도이므로 Ux=U cos(FROM+180−75), Uy=U sin(FROM+180−75).

시험10 동일49표본은 Flexlogger CT30:CU78, 풍속계 높이3m이다. 각 시각의
속도 벡터를 먼저 계산한 평균은 [1.790904, .698400]m/s, 평균 벡터 방위
FROM=276.304311도, 분사축 상대각21.304311도이다. 속도 크기 평균1.990835와
평균벡터 크기1.922264는 다르다. 보고서 표6의 전체시험 평균을 이49초 값으로
대체하지 않는다. 시험23의 원거리1Hz 센서는 p31에서 시험16–23 고장으로
명시되어 사용하지 않았다. 시험23 근거리5분 기록 A76:K78은 N/W/N,
모두1.7m/s이며 하나를 온도에 맞춰 선택하거나 정확한 순간 풍향으로 보지 않는다.
시험10 근거리5분 A55:K56도 NW/N 두 행을 그대로 보존했다.

처음10개 표본은 일부 유량이 존재하여 순수 누출 전 영점 교정이라고 부르지
않는다. 주변온도에서 작은 좌우 차이가 저온 정확도를 입증하지도 않는다.
원본 워크북/센서값/좌표/고정1차134파일은 바꾸지 않았다.

## 모델 항 보완과 규모

선택형 `ambient_vector_exchange.py`는 유입 공기의 질량·수소·3방향 운동량·
총에너지 [m,0,mWx,mWy,mWz,m(ha+|W|²/2)]를 구현한다. 회전공변성,
엔탈피 기준 이동, 0유입, 음의 속도 성분 등을 검사했다. 항력·압력일·TKE를
대체하는 완성3D모델은 아니다.

실제 CONTROL10/23의 저장된 연행량·풍속크기·높이보정·단면은 고정하고 관측
방향만 바꾼 수지 진단에서 시험10 유효49표본 평균 방향의 누락된 횡운동량은
원래 단면 운동량 대비 x=1.19/1.78/4/6m에서2.31/5.49/14.67/20.47%였다.
같은 풍속크기 때문에 에너지 유입 차이는 상대5.22e-16 이하이다. 이 수치는
새 궤적이나 온도 개선이 아니다. 기존 평면 모델에 축방향 투영만 넣고 횡방향
수지를 버리는 수정은 하지 않는다.

## 재현 기록과 다음

- `reference/preslhy/lateral_temperature_symmetry_2026-09-06.json`
- `reference/preslhy/entrainment_wind_vector_2026-09-06/complete.json`
- 두 입력/코드/검사 사전계획은 각각 결과 해시에 봉인했다. 수정시 별도 버전을 쓴다.

기존 코드는 유지한다. 다음은 독립적으로 유효한 시험10 관측 풍향을 쓰는
선택형3D 후보의 0횡풍 극한/6보존량 접속/실제 적분/동일 관측점 비교다.
시험23 고장자료나 임의 편향각으로 모델을 맞추지 않는다. 기존 근접장 중심
잔차는 위치만으로 설명되지 않는다는 앞선 진단도 그대로 유효하다.
