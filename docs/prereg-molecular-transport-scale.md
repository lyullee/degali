# 분자 수송의 기존 필드 대비 규모 — 2026-09-06

기존 소스/열량/PDF 감사의 반복이 아닌 새 물리 규모 검사다. 계수를 조정하거나
난류 수송률을 분자 확산률로 대체하지 않는다. 확산모델 개선 점수를 만들지 않는다.

Van Heijningen(1967) 학위논문 Chapter I, 인쇄11쪽 Table I(PDF20쪽)의
등몰 기체 N2/H2 D*p 다섯 값을 그대로 입력한다. T=65.25/77.35/90.2/
169.3/294.8 K, D*p=4.70/6.71/9.00/28.94/76.64 (10^-2 cm²/s atm).
같은 표의 ±값=.04/.02/.05/.15/.20은 저자 90% 회귀 신뢰구간이며
모든 체계오차를 포함한 정확도나 조성/온도 보간 불확실성은 아니다.
인쇄15쪽에서65 K값은 흡착 의심 때문에 퍼텐셜 적합에 제외했다고 명시했다.
기본 참조 보간은77.35--294.8 K만;65.25--77.35 K는 별도 명시적
잠정 허용 모드에서만 계산한다. log(T)-log(D*p) 선형 보간, 외삽 금지.
순수 소자 Lennard-Jones 평균이나 상온 Cantera NASA 다항식을 저온으로
외삽하지 않는다. 이 표는 실제 습한 혼상 공기의 확산률 표가 아니다.

변경 없는7개 CONTROL 필드의 모든 저장노드/arc_length를 재생한다.
고정 q=0,.5,1,2,4에서 기존 u(q,s)의 역수를 적분해 대류 시간 규모를
얻는다. 이는 고정 q를 따른 조건부 경로이며 실제 횡방향 질량이동을 따른
유선 연령은 아니다. 실제 sigma_y,z와 비교해 참조 이성분 Fick 확산 길이
sqrt(2*D_reference*time), 독립 Gaussian 분산 가산 시 폭 변화율
sqrt(1+2Dt/sigma²)-1 및 폭1%를 만드는 필요 D를 보고한다.
가장 큰 표 값+인쇄 ±값을 전 경로에 상수로 넣은 비교도 별도 표시한다.
이는 표 내부의 큰 참조값이지 임의 혼상/온도/조성의 엄밀한 상한이 아니다.
어떤 확대도 실제 열·성분 분포의 변화로 채택하지 않는다.

원래 EOS의315국소 표본에서 순수 기체로 분류된 값에만 이성분 조건부
Soret 단위 응답을 평가한다. 정의:
j1=-rho*D*(gradY+alpha*Y*(1-Y)*grad(logT)), j2=-j1.
alpha는 부호 있는 명시 입력, 기본값 없음. 단위 alpha의 Soret/Fick 유량
비는 조성·온도 구배로 계산하되 실제 alpha나 난류 효과라고 부르지 않는다.
응축 상태는 개별 개수와 함께 미지원으로 남긴다. 에너지 수송 연결에는
성분 엔탈피 운반과 상호 열수송(Dufour)의 근거가 추가로 필요하며 여기서
자동 완성하지 않는다. 새 연산자의 종합 비음수 엔트로피를 주장하지 않는다.

참조 Fick만으로 기존 폭 변화가 모두1%보다 작고 실제 저온 Soret/혼상
계수가 없으면 이 경로는 전 필드 재적분하지 않고 선별 완료/조건부 미지원으로
닫는다. 1%는 선별을 위한 크기 기준이지 물리/통계 정확도 승인선이 아니다.
기존134파일/원자료 보존, 새 코드/시험/원문/입력 해시 봉인.
