# 좌우 온도 대칭성의 구조적 오차 분리 — 2026-09-06

같은41센서 평균 통계와 좌우 차이를 이미 확인한 뒤 작성한 사후 진단
계획이다. 독립 검증·새 관측·새 모델 계수 추정으로 표시하지 않는다.

동결 JSON의 모든 Left/Right 정확한 짝을 사용한다. 같은 trial,x,z,
서로 반대인 y, 같은 원본 시간/표본 수를 요구한다. Left178/400,
Right178/400가 있어 시험당2쌍, 전체4쌍8센서가 예상되며 일부를 고르지 않는다.
원래 BASE_SINGLE_IMAGE와 CONTROL의 각 짝 예측이 같은지도 검증한다.
같은 min/p05/median과 새 산술평균을 각각 독립적으로 분석한다.

어떤 대칭 예측p에서도 |p-L|+|p-R|>=|L-R|다. 최적 p의 범위는
[min(L,R),max(L,R)], 쌍별 MAE의 하한은|L-R|/2. 제곱오차 최소는
p=(L+R)/2에서 쌍별 RMSE=|L-R|/2다. 이를 모델 예측으로 채택하지 않고
대칭 함수군의 불가피한 오차 하한으로만 저장한다.41개 전체에 대한
기여하한은 sum_pairs|L-R|/41이다. 불일치가 측정/정상성 가정의 영향을
받으므로 ‘물리 오차만’의 하한으로 과대해석하지 않는다.

같은49/98원시 표본을 포함한 mean-audit JSON에서 순간 L-R의 방향,
평균/중앙값/표준편차/분위값을 별도로 보고한다. 개별 센서의 최저값은
동시가 아닐 수 있으므로 두 최저값의 차이를 순간 온도 구배로 읽지 않는다.
시간 상관을 무시한 유의확률/신뢰구간은 계산하지 않는다.

원래 배치 및 풍향 기준과 계측 오차 설명을 원문에서 확인한다. 좌표나
기존 값을 바꾸지 않고, 제트 편향각을 관측 온도에 맞추지 않는다. 근접장
중심의 따뜻한 잔차는 이 좌우 분리만으로 해결됐다고 주장하지 않는다.
새 실제 필드/보존 접속/오차 개선 점수를 만들지 않는다. 원본/134고정
파일은 유지하고 입력·도구·새 검사·계획을 해시로 봉인한다.

## 원문 확인 후, 풍속 벡터 계산 전 추가한 계획

D3.6 31--33쪽: 분사 방위는 북에서 시계방향 약75도, 바람은 불어오는
방향이다. 시험16--23의 원거리1Hz 풍향계는 고장으로 제외한다. 시험10의
Flexlogger CT/CU열은 같은49표본만 읽어 순간 속도 벡터를 먼저 계산한다.
우측 축은 분사축에서 시계방향90도, 따라서
U_x=U*cos(from+180-75), U_right=U*sin(from+180-75).
방위각 산술평균 대신 속도 벡터 평균과 원형 평균을 구분한다.

두 시험 LocalWeather는 전체 기록을 입력에 쓰지 않고, 고정 방출 구간의
직전/구간내/직후 시각에 해당하는 5분 기록을 각 행으로 보존한다. 시간표기가
5분 구간의 시작/끝인지는 확인 전 단정하지 않으며 센서1.5m/원거리3m 높이를
구분한다. 이 거친 기록을 정확한1초 풍향으로 보간하거나 두 관측소 중
온도를 잘 맞추는 쪽을 선택하지 않는다. 고장23값은 겉보기로 변동하더라도
유효 신호로 복원하지 않는다. 유효 풍향이 없는 표본을0도로 메우지 않는다.

센서 영점의 참고로 동일4쌍의 파일 첫10표본을 별도 비교한다. 실제 방출
이전의 ambient 조건/유량을 확인하고 원래 방출 온도값에서 이를 빼는 보정은
하지 않는다. 단일 ambient 일치는 저온 교정의 증거가 아니다.
