# Direction-preserving mass circulation: bounded feasibility pilot

Registered before running trial10 failed endpoint on2026-09-06.

Purpose: determine whether a small conservative redistribution can meet the
existing local edge requirements WITHOUT the20.7% nonradial momentum-stress
component of the independently parameterized witness. This is not a closure,
not an accuracy score and not an adopted downstream ODE.

Keep the same source, failed8-step state, EOS table, prescribed scalar mixing,
profiles, all5 global equations and ALL scalar weak equations. Reuse the
independently phase-resolved original state matrices already verified in
streamed_witness_verification. Preserve every frozen input hash and failure.

Use ONLY4 mass streamfunction modes. Set deltaFM=V and
deltaFP=u V+(a,b)r with r=-integral(grad(u).V)dq/(2q). Both divergences vanish;
deltaFP-u deltaFM remains in the original normalized scalar-stress direction.
This is not a claim of physical-coordinate isotropy. Calculate the changed
production and boundary kinetic-energy work explicitly. Integrate new retained
columns on4/8 and8/16 phase-split quadratures in8-angle batches. Check zero
integrated mass/momentum and global heat correction (absolute1e-7 per unit
amplitude); original global rows must not be silently altered.

On the same16/96 constraint mesh use the earlier4% edge target, production>=0,
inward mass with1e-6 relative margin and curvature*halfwidth<.1. Minimize maximum
mass amplitude normalized by source mass/(16q0), strictly a numerical witness
selector. No retry with different target, different number of modes, or changed
physics after an infeasible result in this pilot.

If both resolutions are feasible, compare rates/amplitudes and new columns
(relative denominator max(abs(value),1), tolerance1e-5); cross-check fine solution
on both retained matrices at1e-8. Independent65 rays/order16 must have all edge
defects<=5%, nonnegative production and projected diffusivity, inward mass, and
nonradial stress fraction<=1e-10. Record all metrics without clipping.

An infeasible answer rules out only this four-mode family, NOT all aligned flows.
A feasible answer leaves circulation amplitudes undetermined as physical laws.
The upstream actual-energy finite-difference failure remains an explicit
adoption blocker even if this separate necessary feasibility screen passes.
Do not score observations, modify defaults or integrate downstream in this pilot.
